import 'dart:ui';

import 'package:flutter/material.dart';

class CustomColor {
  Color primary = Color(0xFF3fa80d);
  Color secondary = Color(0xff71cb4a);

  // generic
  Color white = Color(0xFFFFFFFF);
  Color black = Color(0xFF000000);
  Color background = Color(0xfff0faeb);

  // gray
  Color grayLight = Color(0xFF888888);
  Color grayDefault = Color(0xFF555555);
  Color grayDark = Color(0xFF333333);
  Color grayDarker = Color(0xFF111111);
  Color grayBackground = Color(0xFFF5F5F5);
  Color grayBorder = Color(0xFFEAEAEA);

  // green
  Color greenTint = Color(0xffe9f6e0);
  Color greenLight = Color(0xff71cb4a);
  Color greenDefault = Color(0xFF3fa80d);
  Color greenDark = Color(0xff2e7e07);
  Color greenDarker = Color(0xff205005);

  // red
  Color redDefault = Color(0xffea1218);

  // blue
  Color blueDefault = Color(0xff123dea);

  // purple
  Color purpleDefault = Color(0xff720ee1);

  // ambar
  Color ambarTint = Color(0xFFF7E2CE);
  Color ambarCarignan = Color(0xFFDD7D1E);
}
